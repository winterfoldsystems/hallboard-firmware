# Copy to secrets.yaml (gitignored). Phase 1 removes all of these from the firmware.
rtt_refresh_token: "your-realtime-trains-refresh-token"
cal_url: "https://your-worker.workers.dev/"
cal_token: "shared-device-token"
